# Frontend

Static frontend for GitHub Pages deployment.

## Configure API URL

Edit `config.js` and set:

- `API_BASE`: your Render base URL (example: `https://your-service.onrender.com`)

## Local preview

Open `index.html` directly or use a simple static server.
